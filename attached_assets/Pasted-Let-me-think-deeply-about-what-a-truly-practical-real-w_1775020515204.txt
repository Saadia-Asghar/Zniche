Let me think deeply about what a truly practical, real-world flow looks like before writing anything.Now I have everything I need. Let me design the full improved flow visually first, then write the complete prompt.Now here is the complete, final, improved prompt — every flow improved, every question made practical and human:

---

## 🚀 ZNICHE V3 — COMPLETE REPLIT AGENT 4 PROMPT

Copy this entire block and paste into Agent 4:

---

> **Build Zniche v3** — a skill-to-income marketplace. The entire experience must feel like a premium consumer app, not a hackathon project. Every screen, every question, every transition must feel human, warm, and effortless.
>
> ---
>
> ## PHASE 1 — CONVERSATIONAL ONBOARDING (5 SMART SCREENS)
>
> **Critical rule: NO FORMS. No fields with labels. No dropdowns. No "fill this out" energy. Every screen is ONE question with visual, tappable answers. Selecting an answer auto-advances to the next screen with a 300ms slide animation. A progress dot row (5 dots) sits at the top of every screen.**
>
> ### Screen 1 — "Who are you?"
> Full viewport dark screen. Large friendly heading: *"Hey! What best describes you?"*
> Show 6 large tap cards in a 2×3 grid. Each card has a small SVG icon + label:
> - 🎓 Teacher / Tutor
> - 💼 Professional / Expert
> - 🎨 Creative / Designer
> - 💪 Fitness / Health Coach
> - 👨‍🍳 Chef / Cook
> - 🧑‍💻 Tech / Developer
>
> Each card: `background: #0E0E1C; border: 0.5px solid #1A1A2E; border-radius: 14px; padding: 20px`. On tap: border becomes `2px solid #5B2EFF`, background `#5B2EFF15`. After 300ms: slide to screen 2.
> Store as `user_type` in session.
>
> ### Screen 2 — "What's your skill?"
> Heading: *"What do people come to you for?"*
> Subheading in small muted text: *"Be specific — not 'cooking' but 'Pakistani cooking for beginners'"*
> Single large textarea (no label, just placeholder: *"e.g. I teach Python to complete beginners..."*).
> As user types (after 400ms debounce), call Claude API to generate 4 autocomplete suggestion chips below the textarea:
> ```
> Claude prompt: "The user typed: [INPUT]. Suggest 4 specific skill descriptions (10-15 words each) that someone could monetize. Make them specific and buyer-focused. Return JSON array of 4 strings."
> ```
> Suggestion chips appear below. Tapping one fills the textarea. Character counter: 20–150 chars. "Continue →" pill button appears only when 20+ chars entered.
> Store as `skill_description`.
>
> ### Screen 3 — "How experienced are you?"
> Heading: *"How long have you been doing this?"*
> 4 honest, non-judgmental tap cards:
> - "Learning as I go" (1–2 years)
> - "Pretty confident" (3–5 years)
> - "Very experienced" (6–10 years)
> - "It's my career" (10+ years)
>
> Small text below: *"This helps us price your product correctly — all levels sell well."*
> Store as `experience_level`.
>
> ### Screen 4 — "Who do you want to help?"
> Heading: *"Who is your ideal buyer?"*
>
> **This screen must make a real Claude API call.** Before showing, display a 1-second shimmer skeleton then show 4 AI-generated cards:
> ```
> Claude prompt: "Skill: [skill_description]. Experience: [experience_level]. Generate 4 very specific buyer personas (not generic). Each is a real person with a real problem. Format: [{title: '30-word max', pain_point: '15-word max'}]. Make them feel like real people, not marketing segments."
> ```
> Each card shows the title bold + pain_point in muted text. Plus a 5th card: "Someone else (I'll describe them)" that opens a text field.
> Tapping auto-advances. Store as `target_audience`.
>
> ### Screen 5 — "What format + price?"
> Heading: *"Almost there — how should we package your knowledge?"*
>
> **Another real Claude API call.** Based on all 4 previous answers, Claude recommends:
> ```
> Claude prompt: "Skill: [skill_description], Audience: [target_audience], Experience: [experience_level], User type: [user_type]. Recommend the ONE best product format for this person to start with. Explain why in 15 words. Also recommend a fair price range. Return JSON: {recommended_format: '', reason: '', price_min: 0, price_max: 0, formats: [{name: '', description: '', icon: ''}]} — include all 5 formats but flag which is recommended."
> ```
>
> Show 5 format cards. The recommended one has a `#5B2EFF` border and a small "Recommended for you" pill. Formats: PDF Guide · Live Sessions · Video Course · Template Pack · Toolkit.
>
> Below formats: a custom price slider from $9 to $497. Default shows Claude's recommended midpoint. Real-time display: "You'll earn $XX per sale (after 5% platform fee)". 
> "Build my product →" pill button at bottom. Tapping this starts the build and navigates to the build feed.
>
> ---
>
> ## PHASE 2 — REAL-TIME BUILD FEED (8 STEPS)
>
> **The build feed is the heart of Zniche. It must feel cinematic.**
>
> Layout: Split screen. Left 35%: vertical timeline. Right 65%: live output panel.
>
> **Left panel — vertical timeline:**
> 8 step rows. Each row:
> - Status dot: gray (pending) → purple pulse animation (active) → solid mint (done)
> - Step name (14px, white)
> - Elapsed time counter in muted text (counts up in real-time while active: "1.2s... 2.8s...")
> - One-line result preview appears in green when done
>
> **Right panel — live output:**
> Dark card. Monospace font (`font-family: 'Fira Code', monospace` imported from Google Fonts). Text streams character by character via SSE. Each new step clears the panel with a fade transition and starts streaming. User can scroll up to see previous step results.
>
> **The 8 steps — exact logic:**
>
> **Step 1 — Market Intelligence** (web_search tool ON):
> ```
> System: "You are a market research analyst. Be specific and data-driven. Use web search."
> User: "Search for real evidence of demand for: [skill_description] targeting [target_audience]. Find: (1) What platforms sell this type of knowledge? (2) What are real prices being charged? (3) What search terms do buyers use? Format as 3 clear bullet points with specific numbers and platform names."
> ```
>
> **Step 2 — Buyer Psychology**:
> ```
> System: "You are a buyer psychology expert."
> User: "Based on this audience: [target_audience] who want [skill_description], write a buyer profile. Include: their #1 frustration (1 sentence), what they've already tried (1 sentence), what would make them buy immediately (1 sentence), and their exact first question when they land on a sales page (quoted). Be specific — not generic."
> ```
>
> **Step 3 — Product Architecture**:
> ```
> System: "You are a product designer for digital knowledge products."
> User: "Design a [format] product for [target_audience] taught by someone with [experience_level] experience in [skill_description] priced at $[price]. Create: PRODUCT_NAME (4-6 words, outcome-focused, no fluff), TAGLINE (8-12 words, specific outcome), WHAT'S_INSIDE (5 specific modules or sections with names), UNIQUE_ANGLE (what makes this different from free YouTube content). Return as JSON."
> ```
>
> **Step 4 — Skill Verification** (NEW — not an AI step, it's a USER step):
> Left panel shows this step as "active". Right panel clears and shows:
> - Heading: *"Quick check — 60 seconds"*
> - Muted text: *"We verify creators so buyers trust your product."*
> - Claude generates 3 multiple-choice questions about [skill_description]:
>   ```
>   "Generate 3 practical multiple-choice questions that a genuine [experience_level] expert in [skill_description] would easily answer but a beginner would struggle with. Each question should test real applied knowledge, not just definitions. Return JSON: {questions: [{q, options: [a,b,c,d], correct}]}"
>   ```
> - Questions appear one at a time. User taps an answer. Green flash = correct. Red flash = wrong.
> - Score 3/3 or 2/3: mint checkmark, "Verified Expert" badge earned, step dot goes mint, continue automatically.
> - Score 1/3 or 0/3: warm (not harsh) message: *"Want to tweak your skill description? We'll adjust it to match your actual expertise level."* → offer edit button or continue anyway (product gets "New Creator" badge instead).
>
> **Step 5 — Sales Page Copy**:
> ```
> System: "You are a conversion copywriter. Write like a human, not an AI. Never use the words 'unlock', 'elevate', 'transform', 'journey'."
> User: "Write a complete sales page for: PRODUCT: [product_name], AUDIENCE: [target_audience], PROMISE: [tagline], PRICE: $[price]. Include: HERO_HEADLINE (bold claim, max 8 words, no question marks), HERO_SUBHEADLINE (expands promise, max 18 words), WHO_THIS_IS_FOR (3 bullet points starting with 'You're someone who...'), WHAT_YOU_GET (5 specific items, tangible and concrete), CREATOR_BIO_HOOK (first-person, 2 sentences, mentions experience level naturally), FAQ (3 real questions buyers would have with direct answers), CTA_BUTTON (max 5 words). Return as JSON."
> ```
>
> **Step 6 — Visual Build**:
> - Call Unsplash API: `https://api.unsplash.com/search/photos?query=[skill_description keywords]&per_page=3&orientation=landscape` → pick first result.
> - Generate Three.js product cover: render on `<canvas>` (300×220px), BoxGeometry rounded rect with product name, category color, slow Y-axis rotation.
> - Assemble the product page HTML with all copy + image + Three.js canvas + meta tags + Stripe button.
> - Deploy to `/p/[slug]` route in real time.
> - Show page URL in right panel as a clickable link.
>
> **Step 7 — Stripe Checkout**:
> - Create Stripe Checkout Session server-side:
>   ```javascript
>   const session = await stripe.checkout.sessions.create({
>     payment_method_types: ['card'],
>     line_items: [{price_data: {currency: 'usd', product_data: {name: product_name, description: tagline, images: [unsplash_url]}, unit_amount: price * 100}, quantity: 1}],
>     mode: 'payment',
>     success_url: `${BASE_URL}/purchase/success?session_id={CHECKOUT_SESSION_ID}&product=${slug}`,
>     cancel_url: `${BASE_URL}/p/${slug}`,
>     application_fee_amount: Math.round(price * 100 * 0.05),
>   });
>   ```
> - Store `checkout_url` in DB. Show real payment link in right panel.
>
> **Step 8 — Launch Kit** (NEW):
> ```
> System: "Write like a real person sharing something they're excited about."
> User: "Write a launch kit for [product_name] — [tagline] at $[price]. URL: [page_url]. Create: (1) LinkedIn post (150 words, professional but warm, ends with URL), (2) Twitter/X post (max 240 chars, conversational, 2 hashtags), (3) WhatsApp message (short, feels like texting a friend, no hashtags), (4) Email subject line (max 8 words), (5) Email body (50 words, friendly, one CTA link). Return as JSON."
> ```
> Also generate WhatsApp deep link: `https://wa.me/?text=` + encodeURIComponent(whatsapp_message + ' ' + page_url).
> Also generate Twitter intent URL.
> Also trigger LinkedIn OAuth post if connected.
>
> After step 8: save to DB, send Resend confirmation email to creator, trigger confetti, show celebration screen.
>
> ---
>
> ## PHASE 3 — CELEBRATION SCREEN
>
> Canvas confetti (canvas-confetti CDN). Lottie success animation. Then 4 cards:
>
> Card 1 — "Your live page":
> - Show full URL as large text
> - "Copy link" + "Open page" buttons side by side
> - Small preview of the OG image (Unsplash thumbnail)
>
> Card 2 — "Your payment link":
> - Stripe checkout URL
> - "Copy link" button
> - "Test it yourself" button (opens Stripe checkout in new tab)
>
> Card 3 — "Share & get buyers":
> 4 share buttons in a row:
> - **LinkedIn** → if OAuth connected: one-click post; if not: opens OAuth flow
> - **WhatsApp** → opens `wa.me` deep link (pre-filled message) in new tab
> - **Twitter/X** → opens intent URL in new tab
> - **Copy all captions** → copies all 5 to clipboard as formatted text
>
> Card 4 — "Launch kit":
> 3 tabs: LinkedIn | Twitter | WhatsApp | Email
> Each tab shows the caption + copy button. Tabs styled with active underline in `#5B2EFF`.
>
> Bottom CTA: "See your product in the marketplace →"
>
> ---
>
> ## PHASE 4 — REAL-TIME CREATOR DASHBOARD
>
> **Use WebSockets (Socket.io) for real-time metric updates.**
>
> ```javascript
> // Server emits events
> io.to(userId).emit('new_view', {product_id, country_code, timestamp});
> io.to(userId).emit('new_sale', {product_id, amount, buyer_country});
>
> // Client listens
> socket.on('new_view', ({country_code}) => {
>   showToast(`Someone from ${getFlag(country_code)} just viewed your product`);
>   viewCount.textContent = parseInt(viewCount.textContent) + 1;
> });
> socket.on('new_sale', ({amount}) => {
>   showToast(`You just earned $${amount}! 🎉`);
>   revenue.textContent = (parseFloat(revenue.textContent) + amount).toFixed(2);
>   fireConfetti(); // small burst confetti on sale
> });
> ```
>
> Dashboard tabs:
>
> **Tab 1 — Overview:**
> 4 metric cards (real-time): Total Views · Total Sales · Revenue Earned · Conversion Rate (sales/views as %)
> Revenue chart (last 7 days, using Chart.js from CDN)
> Recent activity feed (last 10 events with country flags and timestamps)
>
> **Tab 2 — My Products:**
> Table: product name | status pill | views | sales | revenue | actions (Edit · Pause · Delete · AI Refresh)
> "Build another product" pill button at top right.
>
> **Tab 3 — AI Coach (NEW — fires after 48h live):**
> Claude analyzes performance and shows:
> ```
> Prompt: "Product: [product_name] at $[price] for [audience]. After 48 hours: [view_count] views, [sale_count] sales, conversion rate [rate]%. The industry average conversion rate for digital products is 1-3%. Give 3 specific, actionable suggestions to improve this product's sales. Each suggestion must be concrete, not generic. Format: {suggestions: [{issue: '', fix: '', impact: 'High/Medium/Low'}]}"
> ```
> Each suggestion has a "Apply this fix" button that either:
> - Opens the product page editor (for copy changes)
> - Auto-applies the fix via Claude (for price changes: shows new price, asks confirm)
>
> **Tab 4 — Social (NEW):**
> Shows LinkedIn connection status (connect button if not connected).
> "Post to LinkedIn" one-click if connected.
> Pre-generated WhatsApp share link.
> History of what was shared and when.
>
> **Tab 5 — Settings:**
> Display name · Bio (shown on marketplace) · Country (flag auto-set, editable) · Profile picture (initials circle default, upload optional)
>
> ---
>
> ## PHASE 5 — PUBLIC PRODUCT PAGE (`/p/[slug]`)
>
> **Zero distractions. Built for conversion.**
>
> Structure (top to bottom):
> 1. Full-width Unsplash hero image (320px height, `object-fit: cover`, dark gradient overlay bottom 50%)
> 2. Product name overlaid on gradient (white, 28px, weight 800)
> 3. Three.js 3D cover canvas (300×220, centered, rotates on load)
> 4. Tagline (20px, muted)
> 5. "Who this is for" — 3 bullets (green checkmark SVGs)
> 6. "What you get" — 5 items in card grid (2 columns mobile, 3 desktop)
> 7. Creator card: initials circle + name + country flag + experience level + "Verified Expert" badge if earned + reputation score
> 8. Reviews section: star average + list of reviews. Each review shows buyer initials + "Verified Purchase" tag.
> 9. FAQ — 3 expandable accordion items
> 10. Sticky buy bar: price + "Buy [product_name] — $[price]" pill button. On mobile: full-width at bottom. On desktop: fixed right sidebar card.
> 11. Live view counter: "🔴 [N] people viewing right now" (WebSocket-powered, real data)
>
> OG meta tags (required for WhatsApp/LinkedIn preview cards):
> ```html
> <meta property="og:title" content="[product_name] — [tagline]">
> <meta property="og:description" content="[hero_subheadline]">
> <meta property="og:image" content="[unsplash_image_url]">
> <meta property="og:url" content="[page_url]">
> <meta name="twitter:card" content="summary_large_image">
> ```
>
> ---
>
> ## PHASE 6 — BUYER PURCHASE FLOW
>
> **After Stripe payment succeeds:**
>
> 1. Stripe webhook fires → `POST /api/webhook/stripe`
> 2. Server records purchase in DB
> 3. Server emits `new_sale` event to creator via Socket.io
> 4. **Within 5 seconds**: Resend sends buyer a delivery email:
>    - Subject: `"Your [product_name] is ready"`
>    - Body: warm, friendly, mentions what they bought and next steps
>    - Contains: download link / booking link / access instructions (depends on format)
> 5. Buyer lands on `/purchase/success?session_id=[id]` — celebratory screen with their purchase details
> 6. **48 hours later**: Resend sends review request email to buyer:
>    - Subject: `"How is [product_name] going?"`
>    - Simple 1–5 star link in email → takes to `/review/[purchase_id]` — no login required
>    - Review posted to product page and creator notified via email + dashboard toast
>
> ---
>
> ## MARKETPLACE (`/marketplace`)
>
> Masonry CSS grid. Filter bar sticky top. Real-time product count: "247 products from 89 creators".
>
> Each card (from top):
> - Three.js 3D mini cover (200×140, paused until IntersectionObserver triggers)
> - Category pill (color-coded: Education=purple, Fitness=mint, Tech=blue, Food=amber)
> - Product name (15px, weight 700)
> - "For: [audience short description]" (muted, 12px)
> - Creator row: initials circle + name + country flag + Verified badge if earned
> - Star rating + review count
> - Price (large, weight 700) + "Buy now" pill button
>
> Featured cards: `border-top: 2px solid #5B2EFF` + small "Featured" pill.
>
> Live ticker above marketplace: *"Amara just earned $29 from Web design templates · 4 min ago"* — pulls last 10 purchases from DB, auto-cycles every 4 seconds.
>
> ---
>
> ## FULL TECH STACK
>
> ```
> Frontend:    React 18 + Vite + Tailwind CSS
> 3D:          Three.js r128 (CDN)
> Charts:      Chart.js (CDN)
> Confetti:    canvas-confetti (CDN)
> Animations:  Lottie Player (CDN)
> Font:        Inter + Fira Code (Google Fonts)
> Real-time:   Socket.io (client + server)
> Streaming:   Server-Sent Events (SSE) for AI output
> Backend:     Node.js + Express
> Database:    PostgreSQL + Drizzle ORM
> Auth:        Replit Auth
> AI:          Anthropic SDK (streaming, web_search tool)
> Payments:    Stripe (Checkout Sessions + Webhooks + Connect)
> Email:       Resend
> Images:      Unsplash API
> Geolocation: ipapi.co/json (country detection)
> Countries:   REST Countries API (flags)
> Social:      LinkedIn OAuth (w_member_social), WhatsApp deep links, Twitter intent URLs
> ```
>
> ---
>
> ## DARK / LIGHT MODE
>
> Store preference in localStorage key `zniche_theme`.
> Toggle button in navbar (moon/sun SVG icon, no text).
> Wrap `<body>` with class `dark` or `light`. All CSS uses `.dark` and `.light` selectors.
> Transition: `body { transition: background-color 0.3s ease, color 0.3s ease; }`
> Default: dark mode.
>
> Dark → Light differences:
> - Background: `#07070E` → `#F8F7FF`
> - Card background: `#0E0E1C` → `#FFFFFF`
> - Card border: `#1A1A2E` → `#E8E4FF`
> - Text primary: `#FFFFFF` → `#0A0A1A`
> - Text secondary: `#888899` → `#5A5A7A`
> - Navbar: `rgba(7,7,14,0.8)` → `rgba(248,247,255,0.85)`
> - All `#5B2EFF` and `#00F0A0` accents stay the same in both modes
>
> ---
>
> ## PERFORMANCE + QUALITY RULES
>
> 1. All Three.js canvases: only animate when visible (`IntersectionObserver`). Pause when off-screen to save CPU.
> 2. All images: `loading="lazy"` attribute.
> 3. Marketplace: virtual scroll or infinite scroll (12 items at a time, load more on scroll).
> 4. All API keys in Replit Secrets. Never in client-side code.
> 5. All monetary values displayed with `toFixed(2)`.
> 6. Mobile breakpoint: everything works at 375px width. Test this.
> 7. All toast notifications auto-dismiss after 4 seconds.
> 8. Error handling: every API call has a try/catch. On failure: show inline retry button for that step only. Never crash the entire build flow.
> 9. Loading states: use shimmer skeleton animation, never spinner for content areas.
> 10. Buttons: never show disabled state without tooltip explaining why.

---

That is the complete, final, production-grade v3 prompt. The key improvements over v2:

The 5 onboarding screens replace every form with a single visual question that auto-advances — no friction, no typing unless needed. Step 4 in the audience screen makes a real Claude call to generate specific buyer personas from the user's own skill description, so every question feels personally relevant. The skill verification quiz is now positioned inside the build flow rather than as a blocker. The dashboard has a live AI Coach tab that fires after 48 hours with specific, actionable fixes. The buyer side now has a full post-purchase loop: 5-second delivery email, 48-hour review request, and real-time creator notifications via WebSocket. Every part of the app is now connected end-to-end.